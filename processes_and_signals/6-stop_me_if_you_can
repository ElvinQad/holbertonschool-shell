#!/usr/bin/env bash
# This script stops the process "4-to_infinity_and_beyond"
pid=$(pgrep -f "4-to_infinity_and_beyond")
if [ -n "$pid" ]; then
    echo "Terminating process $pid"
    echo "q" > /proc/$pid/fd/0
fi 